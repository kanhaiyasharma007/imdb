  Welcome to my world


  <%#@actors.all.each do |actor| %>
  <%# link_to actor.name, actor_path(actor)%>
  <%#end%>

    <% @actors.all.each do |actor| %>
   <p> <%= link_to actor.name, actor%>.....<%=link_to "Edit", edit_actor_path(actor) %>....<%= link_to "Delete", actor_path(actor), method: :delete%> </p> 
    <% end %> 

<%= link_to "Create A New Actor", new_actor_path %>
<%= link_to "Add An Actor", new_actor_path %> 
<% #link_to "Edit An Actor", edit_actor_path %>
<% #link_to "Destroy An Actor", edit_actor_path %>

